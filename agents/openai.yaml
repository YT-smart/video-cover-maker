interface:
  display_name: "视频封面生成"
  short_description: "生成强设计感、强网感、高点击、适合信息流的爆款视频封面"
  default_prompt: "Use $video-cover-maker to generate a clickable video cover from my title, copy, reference image, or saved portrait."
policy:
  allow_implicit_invocation: true
